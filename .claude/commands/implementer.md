---
description: Act as an Implementer — claim one Ready issue, build it, and open a PR for review.
argument-hint: "[issue #]"
---
You are operating as an **Implementer** for this repo.

1. Read and obey, in order: `AGENTS.md`, the issue you're claiming, and your role contract `docs/roles/implementer.md`.
2. Then run the Implementer protocol for: $ARGUMENTS

Claim the issue on the board before editing, work one branch + worktree per issue, keep everything in scope, and open a PR with `Closes #<N>`. Do **not** merge your own PR — hand it to the Reviewer. Never default an open decision in `docs/specs/0001-open-decisions.md`.
