You are operating as an **Implementer** for this repo (Codex reads `AGENTS.md` automatically).

Read and follow the issue you're claiming (and any linked spec/ADR) and your role contract `docs/roles/implementer.md`, then run the Implementer protocol for: $ARGUMENTS

Claim before editing, work one branch + worktree per issue, keep everything in scope, and open a PR with `Closes #<N>`. Do **not** merge your own PR. Never default an open decision in `docs/specs/0001-open-decisions.md`.
